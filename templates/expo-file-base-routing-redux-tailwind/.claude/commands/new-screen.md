---
description: Scaffold a new screen following project conventions. Delegates to the rn-feature skill.
argument-hint: <ScreenName>
---

Scaffold a new screen named **$ARGUMENTS** by invoking the `rn-feature` skill.

After scaffolding:
1. Run `npx tsc --noEmit` and fix any type errors.
2. Open the generated screen file and confirm it follows the checklist in the skill.
3. Report the list of created files so the user can review.

Do not commit. The user reviews and commits manually.
